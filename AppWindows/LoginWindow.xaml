<Window x:Class="AppWindows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login Casa Cambio" Height="400" Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1a1b26"
        Foreground="White"
        FontFamily="Segoe UI">
    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="CASA DE CAMBIO" FontSize="20" FontWeight="Bold" Foreground="#60a5fa" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="Ingresa tus credenciales para conectar el módulo de Emisor." Foreground="#94a3b8" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,30" FontSize="13"/>
            
            <TextBlock Text="Correo Electrónico" Foreground="#94a3b8" Margin="0,0,0,5" FontSize="12"/>
            <TextBox x:Name="TxtEmail" Height="40" Background="#0f172a" Foreground="White" BorderBrush="#334155" Padding="10" Margin="0,0,0,15" />
            
            <TextBlock Text="Contraseña" Foreground="#94a3b8" Margin="0,0,0,5" FontSize="12"/>
            <PasswordBox x:Name="TxtPassword" Height="40" Background="#0f172a" Foreground="White" BorderBrush="#334155" Padding="10" Margin="0,0,0,25" />
            
            <Button x:Name="BtnLogin" Content="Iniciar Sesión" Height="45" Background="#2563eb" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnLogin_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
            
            <TextBlock x:Name="LblError" Foreground="#ef4444" TextWrapping="Wrap" TextAlignment="Center" Margin="0,15,0,0" Visibility="Collapsed" FontSize="12"/>
        </StackPanel>
    </Grid>
</Window>
