<Window x:Class="AppWindows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AssistiveTouch Casa de Cambio" 
        Height="300" Width="300"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent" 
        Topmost="True" 
        ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <Grid>
        <!-- Botón Flotante principal (siempre visible) -->
        <Ellipse x:Name="BtnAssistive" Width="60" Height="60" Fill="#80000000" Stroke="White" StrokeThickness="2" Cursor="Hand" MouseEnter="BtnAssistive_MouseEnter" />
        
        <!-- Menú circular oculto (aparece en hover) -->
        <Grid x:Name="MenuOpciones" Visibility="Collapsed">
            <Border Background="#CC222222" CornerRadius="10" Padding="10" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Casa Cambio" Foreground="White" Margin="0,0,0,5" HorizontalAlignment="Center" FontWeight="Bold"/>
                    <TextBlock Text="Atajos Activos:" Foreground="#94a3b8" Margin="0,0,0,2" FontSize="11" HorizontalAlignment="Center"/>
                    <TextBlock Text="Ctrl+Shift+1 (Entrega)" Foreground="#94a3b8" Margin="0,0,0,2" FontSize="10" HorizontalAlignment="Center"/>
                    <TextBlock Text="Ctrl+Shift+2 (Recibe)" Foreground="#94a3b8" Margin="0,0,0,10" FontSize="10" HorizontalAlignment="Center"/>
                    <Button Content="Cerrar Sesión" Click="BtnLogout_Click" Background="#ef4444" Foreground="White" Padding="5" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
