name: Build Electron Windows App

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Check out Git repository
      uses: actions/checkout@v4

    - name: Install Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20

    - name: Install Dependencies
      run: npm install
      working-directory: ./windows-app

    - name: Build Electron App (Portable)
      run: npx electron-builder --win --x64 > build_log.txt 2>&1
      working-directory: ./windows-app
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      continue-on-error: true

    - name: Upload Logs on Failure
      uses: actions/upload-artifact@v4
      if: failure()
      with:
        name: debug-logs
        path: windows-app/build_log.txt

    - name: Upload Installer EXE
      uses: actions/upload-artifact@v4
      with:
        name: EmisorCasaCambio-Windows
        path: windows-app/dist/EmisorCasaCambio-Setup.exe
